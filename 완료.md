# 완료.md

> `AGENTS.md` 기준으로 완료(또는 워킹트리 반영)된 백로그 항목을 이관 기록으로 관리합니다.  
> 목적은 “무엇이 되었는지, 어디가 바뀌었는지, 다음엔 무엇을 보면 되는지”를 빠르게 파악하는 것입니다.

---

## 상태 표기 기준

- `작업됨 (커밋 반영)`: 원격/로컬 커밋에 반영된 항목
- `작업됨 (워킹트리 반영)`: 파일 반영은 되었으나 커밋 전일 수 있는 항목

---

## P0 작업 이관

### 1) Package Center Installed-tab runtime/lifecycle console polish
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `client/src/apps/package-center/PackageCenter.svelte`
  - `client/src/apps/package-center/api.js`
  - `server/routes/packages.js`
- 핵심:
  - Installed 운영 콘솔(런타임/라이프사이클/헬스/이벤트/로그/백업/롤백) 밀도 강화
  - 설치 앱 운영 액션 흐름 정리
  - 백엔드 응답 계약과 UI 표시 정합성 개선
- 참고 커밋:
  - `a105038`

### 2) Install/update preflight review
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
  - `client/src/apps/package-center/api.js`
- 핵심:
  - 권한/품질게이트/의존성/호환성/백업 preflight 요약 연결
  - blocked 기반 차단 및 승인 흐름 정리
  - 실패 코드/사유 노출 강화
- 참고 커밋:
  - `a105038`

### 3) Settings and Control Panel role separation
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `client/src/apps/settings/Settings.svelte`
  - `client/src/apps/settings/api.js`
  - `client/src/apps/control-panel/ControlPanel.svelte`
- 핵심:
  - Settings(서버/런타임 설정)과 Control Panel(개인화) 역할 분리
- 참고 커밋:
  - `a105038`

### 4) File Station boundary and error UX
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `server/routes/fs.js`
  - `client/src/apps/file-explorer/FileExplorer.svelte`
  - `client/src/apps/file-explorer/api.js`
- 핵심:
  - 허용 루트/경계 기반 실패 처리 및 복구 안내 강화
  - 명시적 오류 코드와 사용자 메시지 개선
- 참고 커밋:
  - `a105038`

### 5) official test command for `server/tests/*`
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `package.json`
- 핵심:
  - 서버 테스트 공식 실행 커맨드 연결
- 참고 커밋:
  - `a105038`

### 6) Start Menu base
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/components/StartMenu.svelte`
  - `client/src/core/stores/startMenuStore.js`
  - `client/src/core/Desktop.svelte`
  - `client/src/core/components/Taskbar.svelte`

### 7) Taskbar settings model
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/taskbarStore.js`
  - `client/src/apps/control-panel/ControlPanel.svelte`
  - `client/src/core/components/Taskbar.svelte`
  - `server/services/stateStore.js`

### 8) Window defaults model
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/windowDefaultsStore.js`
  - `client/src/core/stores/windowStore.js`
  - `client/src/core/Window.svelte`
  - `client/src/apps/control-panel/ControlPanel.svelte`
  - `server/services/stateStore.js`

### 9) Agent chat panel base
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/components/AgentChatPanel.svelte`
  - `client/src/core/components/Agent.svelte`
  - `client/src/core/stores/agentStore.js`

---

## P1 작업 이관

### 0) Code Editor and Package Center file editing integration
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
  - `client/src/apps/package-center/api.js`
  - `client/src/apps/code-editor/CodeEditor.svelte`
  - `client/src/apps/code-editor/api.js`
- 핵심:
  - Package Files 섹션 추가 및 Code Editor 컨텍스트(`appId`, `path`) 연동
  - package 파일 모드 read/write 지원
  - `manifest.json` 우회 수정/생성 차단

### 1) Package creation wizard
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/api.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
- 핵심:
  - `POST /api/packages/wizard/preflight`
  - `POST /api/packages/wizard/create`
  - Draft -> Review -> Create 플로우와 blocker 기반 차단 UX

### 2) manifest editor UI
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/api.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
- 핵심:
  - Manifest Editor UI + preflight/save 흐름
  - `POST /api/packages/:id/manifest/preflight`
  - `PUT /api/packages/:id/manifest`

### 3) Theme presets
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `client/src/core/stores/themePresetStore.js`
  - `client/src/apps/control-panel/ControlPanel.svelte`

### 4) desktop layout persistence and edit mode
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `client/src/core/stores/desktopStore.js`
  - `client/src/core/components/Taskbar.svelte`
  - `client/src/core/Desktop.svelte`

### 5) Agent status expansion and approval cards
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/agentStore.js`
  - `client/src/core/components/Agent.svelte`
  - `client/src/core/components/AgentChatPanel.svelte`
  - `server/services/stateStore.js`

### 6) Wrapped Assistant Mode UI skeleton
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/agentStore.js`
  - `client/src/core/components/AgentChatPanel.svelte`
  - `server/services/stateStore.js`

---

## P2 작업 이관

### 1) Context menu customization
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `client/src/core/stores/contextMenuStore.js`
  - `client/src/core/components/ContextMenu.svelte`
  - `client/src/core/Desktop.svelte`
  - `client/src/apps/control-panel/ControlPanel.svelte`

### 2) app-specific window backgrounds
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `client/src/core/stores/windowDefaultsStore.js`
  - `client/src/core/Window.svelte`
  - `client/src/apps/control-panel/ControlPanel.svelte`

### 3) Docker logs/ports/volumes/Compose
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/docker.js`
  - `client/src/apps/docker-manager/api.js`
  - `client/src/apps/docker-manager/DockerManager.svelte`

### 4) Media playlist/background audio
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/media.js`
  - `server/services/mediaService.js`
  - `client/src/apps/media-player/api.js`
  - `client/src/apps/media-player/MediaPlayer.svelte`

### 5) Document Viewer controls/search
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/apps/document-viewer/DocumentViewer.svelte`
  - `client/src/apps/document-viewer/api.js`

### 6) Model Viewer advanced inspection
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/apps/model-viewer/ModelViewer.svelte`

---

## P3 작업 이관

### 1) WebDAV/cloud write and mount status
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/cloud.js`
  - `server/services/cloudService.js`
  - `client/src/apps/settings/CloudManager.svelte`
  - `client/src/apps/settings/api.js`

### 2) backup job manager
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `server/routes/system.js`
  - `client/src/apps/settings/api.js`
  - `client/src/apps/settings/Settings.svelte`
  - `client/src/apps/settings/BackupJobManager.svelte`

### 3) service/runtime/package dashboard
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/apps/resource-monitor/api.js`
  - `client/src/apps/resource-monitor/ResourceMonitor.svelte`

### 4) download/transfer manager
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/transferJobService.js`
  - `server/routes/transfer.js`
  - `server/index.js`
  - `client/src/apps/transfer/api.js`
  - `client/src/apps/transfer/TransferUI.svelte`

### 5) Docker packaging and portability
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `docker-compose.yml`
  - `docker/Dockerfile.backend`
  - `docker/Dockerfile.frontend`
  - `.dockerignore`
  - `client/vite.config.js`
  - `README.md`
  - `media/.gitkeep`

---

## P4 작업 이관

### 1) Media Library boundary model (Inventory vs User Media)
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `server/utils/mediaLibraryPaths.js`
  - `server/index.js`
  - `server/routes/system.js`
  - `client/src/apps/control-panel/api.js`
  - `client/src/apps/control-panel/ControlPanel.svelte`
- 핵심:
  - Inventory와 사용자 Media Library 경계를 운영 모델로 분리
  - Media Library 정적 서빙 경로(`/api/media-library-files/*`) 연결

### 2) Browse Local Files -> Import to Library UX unification
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `client/src/apps/control-panel/ControlPanel.svelte`
  - `client/src/apps/control-panel/api.js`
- 핵심:
  - Personalization에서 로컬 탐색 후 import 기반 적용 흐름 통합

### 3) media-library API contract and explicit `MEDIA_LIBRARY_*` errors
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `server/routes/system.js`
- 핵심:
  - wallpapers list/upload/import API 계약 정리
  - `MEDIA_LIBRARY_*` 오류 코드 명시화

### 4) package manifest media scopes + approval/audit integration
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/packages.js`
  - `server/services/packageRegistryService.js`
  - `client/src/apps/package-center/api.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
- 핵심:
  - `manifest.media.scopes` 정규화/검증
  - preflight의 `mediaScopeReview` 및 승인 요구 상태 반영
  - 승인 미충족 시 manifest 저장 차단
  - audit 로그에 media scope 변경/승인 메타 반영

### 5) migration guide for legacy wallpaper/media path references
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `MEDIA_LIBRARY_MIGRATION.md`
  - `README.md`
- 핵심:
  - 레거시 경로 -> Media Library 표준 경로 전환 절차 문서화
  - README에 마이그레이션 가이드 링크 추가

---

## 참고

- 커밋 반영 주요 레퍼런스:
  - `a105038` (P0 신뢰성/경계 및 테스트 커맨드 관련)
  - `91f8f06`, `4ab10d8`, `34f154c`, `a9e136e` (P1~P4 관련 확장 묶음)
- 워킹트리 반영 항목은 커밋 시점 기준으로 상태(`커밋 반영`)로 갱신합니다.
