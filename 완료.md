# 완료.md

이 문서는 `AGENTS.md`의 P0~P1 백로그 중 **작업된 항목**을 이관해 둔 기록입니다.
라우팅/서비스/스토어/UI 변경 경로와 핵심 내용을 빠르게 확인할 수 있도록 정리합니다.

## P0 작업 이관

### 1) Package Center Installed-tab runtime/lifecycle console polish
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `client/src/apps/package-center/PackageCenter.svelte`
  - `client/src/apps/package-center/api.js`
  - `server/routes/packages.js`
- 내용:
  - Installed 탭 운영 콘솔(런타임/라이프사이클/헬스/이벤트/로그/백업/롤백) 밀도 보강
  - 설치 앱 운영 정보 조회 및 액션 흐름 정리
  - 백엔드 응답 계약을 클라이언트 운영 콘솔과 맞춤
- 참고 커밋:
  - `a105038` (Implement P0 reliability updates and establish server test command)

### 2) Install/update preflight review: permissions, quality gate, dependency, compatibility, backup
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
  - `client/src/apps/package-center/api.js`
- 내용:
  - registry install/update preflight에 권한/품질게이트/의존성/호환성/백업 요약 연결
  - preflight 결과 기반 차단(blocked) 및 승인 흐름 정리
  - 사용자에게 실패 코드와 사유 노출 개선
- 참고 커밋:
  - `a105038`

### 3) Settings and Control Panel role separation
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `client/src/apps/settings/Settings.svelte`
  - `client/src/apps/settings/api.js`
  - `client/src/apps/control-panel/ControlPanel.svelte`
- 내용:
  - Settings와 Control Panel의 역할 분리 방향으로 UI/동작 정리
  - 사용자 커스터마이징 축은 Control Panel 중심으로 정리
- 참고 커밋:
  - `a105038`

### 4) File Station boundary and error UX
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `server/routes/fs.js`
  - `client/src/apps/file-explorer/FileExplorer.svelte`
  - `client/src/apps/file-explorer/api.js`
- 내용:
  - 경계/허용 루트 기반 파일 접근 실패 처리 개선
  - 명시적 오류 코드와 사용자 메시지 UX 보강
  - 대용량/실패 가능 작업에서 복구 가능한 안내 강화
- 참고 커밋:
  - `a105038`

### 5) official test command for `server/tests/*`
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `package.json`
- 내용:
  - 서버 테스트 공식 실행 스크립트 연결(고아 테스트 방지)
- 참고 커밋:
  - `a105038`

### 6) Start Menu base
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/components/StartMenu.svelte`
  - `client/src/core/stores/startMenuStore.js`
  - `client/src/core/Desktop.svelte`
  - `client/src/core/components/Taskbar.svelte`
- 내용:
  - Start Menu 기본 열기/닫기/앱 목록 표시 기반 구축
  - Desktop/Taskbar에서 Start Menu 트리거 및 상태 연결

### 7) Taskbar settings model
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/taskbarStore.js`
  - `client/src/apps/control-panel/ControlPanel.svelte`
  - `client/src/core/components/Taskbar.svelte`
  - `server/services/stateStore.js`
- 내용:
  - Taskbar 설정 모델(표시/레이아웃/행동 옵션) 추가
  - Control Panel 설정과 실제 Taskbar 렌더링 상태 연동
  - 시스템 상태 저장소를 통한 지속성 연결

### 8) Window defaults model
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/windowDefaultsStore.js`
  - `client/src/core/stores/windowStore.js`
  - `client/src/core/Window.svelte`
  - `client/src/apps/control-panel/ControlPanel.svelte`
  - `server/services/stateStore.js`
- 내용:
  - 기본 창 동작/스타일 설정 모델 추가
  - windowStore 및 Window 렌더링에 기본값 적용
  - Control Panel에서 조정 가능한 기본 창 모델과 저장 연동

### 9) Agent chat panel base
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/components/AgentChatPanel.svelte`
  - `client/src/core/components/Agent.svelte`
  - `client/src/core/stores/agentStore.js`
- 내용:
  - Agent 채팅 패널 기본 UI/상태 흐름 추가
  - Agent 컴포넌트와 스토어 상태 연동 기반 마련

## P1 작업 이관

### 1) Package creation wizard
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/api.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
- 내용:
  - Wizard 전용 API 추가:
    - `POST /api/packages/wizard/preflight`
    - `POST /api/packages/wizard/create`
  - Draft -> Review(preflight) -> Create UI 플로우 추가
  - preflight blocker 기반 생성 차단 UX 반영

### 2) manifest editor UI
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/api.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
- 내용:
  - Manifest Editor UI(Installed ops 내) 추가
  - manifest 로드/JSON 편집/파싱 오류 표시/Preflight/Save 플로우 구현
  - preflight API 추가:
    - `POST /api/packages/:id/manifest/preflight`
  - 저장 API 연동:
    - `PUT /api/packages/:id/manifest`

## 참고
- 커밋 반영된 묶음:
  - `a105038` (P0 신뢰성/경계 및 테스트 커맨드 관련 작업)
- 워킹트리 반영(미커밋 포함) 항목은 추후 커밋 시점 기준으로 갱신 필요
