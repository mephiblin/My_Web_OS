# 완료.md

이 문서는 `AGENTS.md`의 P0~P1 백로그 중 **작업된 항목**을 이관해 둔 기록입니다.
라우팅/서비스/스토어/UI 변경 경로와 핵심 내용을 빠르게 확인할 수 있도록 정리합니다.

## P0 작업 이관

### 1) Package Center Installed-tab runtime/lifecycle console polish
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `client/src/apps/package-center/PackageCenter.svelte`
  - `client/src/apps/package-center/api.js`
  - `server/routes/packages.js`
- 내용:
  - Installed 탭 운영 콘솔(런타임/라이프사이클/헬스/이벤트/로그/백업/롤백) 밀도 보강
  - 설치 앱 운영 정보 조회 및 액션 흐름 정리
  - 백엔드 응답 계약을 클라이언트 운영 콘솔과 맞춤
- 참고 커밋:
  - `a105038` (Implement P0 reliability updates and establish server test command)

### 2) Install/update preflight review: permissions, quality gate, dependency, compatibility, backup
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
  - `client/src/apps/package-center/api.js`
- 내용:
  - registry install/update preflight에 권한/품질게이트/의존성/호환성/백업 요약 연결
  - preflight 결과 기반 차단(blocked) 및 승인 흐름 정리
  - 사용자에게 실패 코드와 사유 노출 개선
- 참고 커밋:
  - `a105038`

### 3) Settings and Control Panel role separation
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `client/src/apps/settings/Settings.svelte`
  - `client/src/apps/settings/api.js`
  - `client/src/apps/control-panel/ControlPanel.svelte`
- 내용:
  - Settings와 Control Panel의 역할 분리 방향으로 UI/동작 정리
  - 사용자 커스터마이징 축은 Control Panel 중심으로 정리
- 참고 커밋:
  - `a105038`

### 4) File Station boundary and error UX
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `server/routes/fs.js`
  - `client/src/apps/file-explorer/FileExplorer.svelte`
  - `client/src/apps/file-explorer/api.js`
- 내용:
  - 경계/허용 루트 기반 파일 접근 실패 처리 개선
  - 명시적 오류 코드와 사용자 메시지 UX 보강
  - 대용량/실패 가능 작업에서 복구 가능한 안내 강화
- 참고 커밋:
  - `a105038`

### 5) official test command for `server/tests/*`
- 상태: 작업됨 (커밋 반영)
- 경로:
  - `package.json`
- 내용:
  - 서버 테스트 공식 실행 스크립트 연결(고아 테스트 방지)
- 참고 커밋:
  - `a105038`

### 6) Start Menu base
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/components/StartMenu.svelte`
  - `client/src/core/stores/startMenuStore.js`
  - `client/src/core/Desktop.svelte`
  - `client/src/core/components/Taskbar.svelte`
- 내용:
  - Start Menu 기본 열기/닫기/앱 목록 표시 기반 구축
  - Desktop/Taskbar에서 Start Menu 트리거 및 상태 연결

### 7) Taskbar settings model
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/taskbarStore.js`
  - `client/src/apps/control-panel/ControlPanel.svelte`
  - `client/src/core/components/Taskbar.svelte`
  - `server/services/stateStore.js`
- 내용:
  - Taskbar 설정 모델(표시/레이아웃/행동 옵션) 추가
  - Control Panel 설정과 실제 Taskbar 렌더링 상태 연동
  - 시스템 상태 저장소를 통한 지속성 연결

### 8) Window defaults model
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/windowDefaultsStore.js`
  - `client/src/core/stores/windowStore.js`
  - `client/src/core/Window.svelte`
  - `client/src/apps/control-panel/ControlPanel.svelte`
  - `server/services/stateStore.js`
- 내용:
  - 기본 창 동작/스타일 설정 모델 추가
  - windowStore 및 Window 렌더링에 기본값 적용
  - Control Panel에서 조정 가능한 기본 창 모델과 저장 연동

### 9) Agent chat panel base
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/components/AgentChatPanel.svelte`
  - `client/src/core/components/Agent.svelte`
  - `client/src/core/stores/agentStore.js`
- 내용:
  - Agent 채팅 패널 기본 UI/상태 흐름 추가
  - Agent 컴포넌트와 스토어 상태 연동 기반 마련

## P1 작업 이관

### 0) Code Editor and Package Center file editing integration
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
  - `client/src/apps/package-center/api.js`
  - `client/src/apps/code-editor/CodeEditor.svelte`
  - `client/src/apps/code-editor/api.js`
- 내용:
  - Installed Ops Console에 Package Files 섹션(디렉터리 진입/상위 이동/재조회) 추가
  - 파일 엔트리에서 Code Editor로 패키지 파일 컨텍스트(`appId`, `path`) 전달 연동
  - Code Editor에 host FS 모드 + package 파일 모드(read/write) 병행 지원
  - backend generic package file mutation에서 `manifest.json` 우회 수정/생성 차단

### 1) Package creation wizard
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/api.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
- 내용:
  - Wizard 전용 API 추가:
    - `POST /api/packages/wizard/preflight`
    - `POST /api/packages/wizard/create`
  - Draft -> Review(preflight) -> Create UI 플로우 추가
  - preflight blocker 기반 생성 차단 UX 반영

### 2) manifest editor UI
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/packages.js`
  - `client/src/apps/package-center/api.js`
  - `client/src/apps/package-center/PackageCenter.svelte`
- 내용:
  - Manifest Editor UI(Installed ops 내) 추가
  - manifest 로드/JSON 편집/파싱 오류 표시/Preflight/Save 플로우 구현
  - preflight API 추가:
    - `POST /api/packages/:id/manifest/preflight`
  - 저장 API 연동:
    - `PUT /api/packages/:id/manifest`

### 3) Theme presets
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `client/src/core/stores/themePresetStore.js`
  - `client/src/apps/control-panel/ControlPanel.svelte`
- 내용:
  - 시스템 상태 저장소에 `themePresets` 키 추가 및 검증/정규화 로직 확장
  - Control Panel 개인화 탭에 테마 프리셋 저장/적용/삭제 UI 추가
  - 현재 테마(블러/투명도/강조색/배경 타입·소스·fit)를 프리셋으로 저장하고 즉시 적용 가능하도록 연동

### 4) desktop layout persistence and edit mode
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `client/src/core/stores/desktopStore.js`
  - `client/src/core/components/Taskbar.svelte`
  - `client/src/core/Desktop.svelte`
- 내용:
  - 데스크톱 상태 저장 모델에 `layoutEditMode`를 추가하고 `desktops` 상태 영속화 연결
  - `desktopStore`를 `init/save/toggle` 가능한 영속 스토어로 확장
  - Taskbar에 Desktop Layout Edit 토글 버튼 추가
  - Layout Edit Mode 활성화 시 데스크톱 아이콘 실행을 잠시 비활성화하고 상태 배너 표시

### 5) Agent status expansion and approval cards
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/agentStore.js`
  - `client/src/core/components/Agent.svelte`
  - `client/src/core/components/AgentChatPanel.svelte`
  - `server/services/stateStore.js`
- 내용:
  - Agent 상태 모델을 `idle/listening/thinking/executing/success/warning/error/terminal`로 확장
  - 채팅 메시지 스키마에 `kind/approval/meta` 필드를 추가하고 영속화 정규화 반영
  - 리스크 키워드 입력 시 승인 카드 생성, 승인/거절 액션 및 결과 메시지 흐름 추가
  - 아바타 아이콘/색상을 새 상태 모델에 맞게 갱신

### 6) Wrapped Assistant Mode UI skeleton
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/core/stores/agentStore.js`
  - `client/src/core/components/AgentChatPanel.svelte`
  - `server/services/stateStore.js`
- 내용:
  - Agent Chat 패널에 Wrapped Assistant Mode 토글/플래너 UI 스켈레톤 추가
  - 계획 액션 리스트 추가/초기화/승인 기반 실행 요청(`Run With Approvals`) 흐름 연결
  - `agentChat` 상태에 `wrappedMode(enabled/intentDraft/plannedActions)` 영속화 정규화 확장

## P2 작업 이관

### 1) Context menu customization
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `client/src/core/stores/contextMenuStore.js`
  - `client/src/core/components/ContextMenu.svelte`
  - `client/src/core/Desktop.svelte`
  - `client/src/apps/control-panel/ControlPanel.svelte`
- 내용:
  - `contextMenu` 상태 키(아이콘 표시/위험 액션 확인/밀도) 추가 및 영속화 정규화 확장
  - Context Menu 렌더링에 밀도(`compact/cozy`) 및 아이콘 표시 토글 반영
  - 위험 액션 실행 전 확인(confirm) 플로우 추가
  - Control Panel에서 Context Menu 커스터마이징 설정 UI 추가

### 2) app-specific window backgrounds
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `client/src/core/stores/windowDefaultsStore.js`
  - `client/src/core/Window.svelte`
  - `client/src/apps/control-panel/ControlPanel.svelte`
- 내용:
  - `windowDefaults` 모델에 `appBackgrounds` 맵을 추가하고 영속화 정규화 확장
  - Window 렌더링에서 앱 ID별 배경 스타일(`appBackgrounds[appId]`) 적용
  - Control Panel에 앱별 배경 추가/불러오기/삭제 UI 추가

### 3) Docker logs/ports/volumes/Compose
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/docker.js`
  - `client/src/apps/docker-manager/api.js`
  - `client/src/apps/docker-manager/DockerManager.svelte`
- 내용:
  - Docker API에 로그(`GET /api/docker/logs`), 볼륨(`GET /api/docker/volumes`), Compose 프로젝트(`GET /api/docker/compose/projects`) 조회 추가
  - Docker Manager에 컨테이너 로그 패널, 볼륨 패널, Compose 프로젝트 패널 추가
  - 기존 컨테이너 목록의 포트 표시를 유지하면서 운영 콘솔 밀도 확장

### 4) Media playlist/background audio
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/media.js`
  - `server/services/mediaService.js`
  - `client/src/apps/media-player/api.js`
  - `client/src/apps/media-player/MediaPlayer.svelte`
- 내용:
  - Media API에 같은 폴더 기준 플레이리스트 조회(`GET /api/media/playlist`) 추가
  - 플레이리스트 API 응답에 `currentPath/currentIndex/total/items[{path,name,ext,kind}]` 계약 반영
  - 플레이리스트 관련 명시적 오류 코드(`MEDIA_PLAYLIST_*`) 추가
  - Media Player에 플레이리스트 패널, 반복(`off/all/one`), 셔플, 종료 후 자동 다음 트랙 재생 추가
  - Background Audio 토글 추가:
    - `on`: 비활성/최소화 상태에서도 재생 유지
    - `off`: 비활성 또는 최소화 시 자동 일시정지
  - 기존 이미지 뷰어 줌/이전/다음 동작은 유지

### 5) Document Viewer controls/search
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/apps/document-viewer/DocumentViewer.svelte`
  - `client/src/apps/document-viewer/api.js`
- 내용:
  - Document Viewer 툴바에 페이지 이동/줌 인·아웃/리셋/전체화면/다운로드 컨트롤 추가
  - PDF의 경우 iframe hash(`page`, `zoom`, `search`)를 이용한 기본 네비게이션 컨트롤 연동
  - 텍스트 문서(`txt/md/json/js/ts/css/html/xml/yaml/csv/log`)는 `/api/fs/read`로 직접 로드해 뷰어 렌더링
  - 텍스트 검색어 하이라이트와 이전/다음 매치 이동 UI 추가
  - 앱 로컬 API helper(`document-viewer/api.js`)로 문서 읽기 호출 분리

### 6) Model Viewer advanced inspection
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/apps/model-viewer/ModelViewer.svelte`
- 내용:
  - Model Viewer 제어 패널에 와이어프레임 토글, 축(Axes) 토글, 인스펙터 토글, 스크린샷 저장 버튼 추가
  - 로드된 모델 기준 메시 수, 삼각형 수, 재질 목록(이름/타입/색상/맵 유무) 집계 및 인스펙터 패널 표시
  - 모델 재로딩 시 기존 모델 제거/재배치 및 와이어프레임 상태 재적용 처리
  - `data.path` 변경 시 모델 재로딩하도록 반응형 로드 흐름 보강

## 참고
- 커밋 반영된 묶음:
  - `a105038` (P0 신뢰성/경계 및 테스트 커맨드 관련 작업)
- 워킹트리 반영(미커밋 포함) 항목은 추후 커밋 시점 기준으로 갱신 필요

## P3 작업 이관

### 1) WebDAV/cloud write and mount status
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/routes/cloud.js`
  - `server/services/cloudService.js`
  - `client/src/apps/settings/CloudManager.svelte`
  - `client/src/apps/settings/api.js`
- 내용:
  - Cloud API에 `POST /api/cloud/mount`, `GET /api/cloud/mount-status`, `POST /api/cloud/write` 추가
  - 마운트 상태 모델(`mounted/unmounted/error`)과 URL/세부정보 응답 계약 추가
  - `rclone rcat` 기반 텍스트 업로드(write) 구현 및 입력 검증(원격/경로/5MB 제한/오류 코드) 추가
  - `GET /api/cloud/remotes` 응답에 mount 상태 병합(`mountStatus/mountUrl/mounted`)하여 UI 연동 보강
  - CloudManager를 API helper 기반으로 정리하고 mount 상태 표시/새로고침/원격 write test UI 추가

### 2) backup job manager
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/stateStore.js`
  - `server/routes/system.js`
  - `client/src/apps/settings/api.js`
  - `client/src/apps/settings/Settings.svelte`
  - `client/src/apps/settings/BackupJobManager.svelte`
- 내용:
  - 시스템 상태 저장소에 `backupJobs` 키 추가(`jobs/history` 정규화 포함)
  - Backup Job API 추가:
    - `GET /api/system/backup-jobs`
    - `POST /api/system/backup-jobs`
    - `DELETE /api/system/backup-jobs/:id`
    - `POST /api/system/backup-jobs/:id/run`
  - 백업 실행 시 실제 로컬 파일/디렉터리 복사 수행(`fs-extra.copy`) 및 실행 이력 기록
  - source/destination 허용 루트 검증, 시스템 인벤토리 보호 경로 차단, 명시적 `BACKUP_JOB_*` 오류 코드 적용
  - Settings 앱에 `Backup Jobs` 탭과 관리 UI(생성/실행/삭제/최근 이력) 추가

### 3) service/runtime/package dashboard
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `client/src/apps/resource-monitor/api.js`
  - `client/src/apps/resource-monitor/ResourceMonitor.svelte`
- 내용:
  - Resource Monitor에 `Ops` 탭 추가
  - 운영 대시보드에서 서비스(`/api/services`), 런타임 앱(`/api/runtime/apps`), 설치 패키지(`/api/packages`) 상태를 통합 조회
  - 서비스/런타임/패키지 요약 카드(총량/실행중/에러) 추가
  - 서비스 테이블(이름/상태/업타임), 런타임 테이블(App/상태/PID) 추가
  - Refresh Ops 액션과 주기 갱신 루프를 통해 운영 상태 가시성 강화

### 4) download/transfer manager
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `server/services/transferJobService.js`
  - `server/routes/transfer.js`
  - `server/index.js`
  - `client/src/apps/transfer/api.js`
  - `client/src/apps/transfer/TransferUI.svelte`
- 내용:
  - Transfer Job API 추가:
    - `GET /api/transfer/jobs`
    - `POST /api/transfer/jobs/download`
    - `POST /api/transfer/jobs/copy`
    - `POST /api/transfer/jobs/:id/cancel`
  - URL 다운로드/로컬 파일 복사 작업 큐(FIFO) 및 진행률/상태 추적(`queued/running/completed/failed/canceled`) 구현
  - source/destination에 대해 허용 루트 검증, 인벤토리 보호 경로 차단, 명시적 `TRANSFER_*` 오류 코드 적용
  - Transfer 앱을 시뮬레이션 UI에서 운영 콘솔형 UI로 전환(작업 생성/목록/진행률/오류/취소/자동 새로고침)

### 5) Docker packaging and portability
- 상태: 작업됨 (워킹트리 반영)
- 경로:
  - `docker-compose.yml`
  - `docker/Dockerfile.backend`
  - `docker/Dockerfile.frontend`
  - `.dockerignore`
  - `client/vite.config.js`
  - `README.md`
  - `media/.gitkeep`
- 내용:
  - Backend/Frontend 컨테이너 실행을 위한 Dockerfile 및 compose 구성 추가
  - 스토리지 영속화 전략:
    - named volume `webos_storage` -> `/app/server/storage`
  - 호스트 바인드 경로 전략:
    - `./data` -> `/workspace/data`
    - `./media` -> `/workspace/media`
  - 프론트 개발 서버 프록시 타깃을 환경변수(`VITE_DEV_PROXY_TARGET`)로 분리하여 컨테이너 네트워크(`backend:3000`) 대응
  - compose 로그 로테이션 옵션(`max-size`, `max-file`)과 실행/중지/로그 확인 가이드 문서화
