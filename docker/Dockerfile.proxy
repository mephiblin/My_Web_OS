FROM node:20-bookworm-slim AS frontend-build

WORKDIR /app/client

COPY client/package.json client/package-lock.json ./
RUN npm ci

COPY client ./
RUN npm run build

FROM caddy:2.10.2-alpine

COPY docker/Caddyfile.hardened /etc/caddy/Caddyfile
COPY --from=frontend-build /app/client/dist /srv
